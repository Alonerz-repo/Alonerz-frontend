import React from "react";
import styled from "styled-components";

const four_four = (props: any) => {
  console.log(props);
  return (
    <React.Fragment>
      <h1>나는 404이다</h1>
      <p>나는 에러다</p>
    </React.Fragment>
  );
};

export default four_four;
